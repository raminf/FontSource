# Copy to `.env` for local tooling only. CI uses GitHub repository secrets with the same names.
# Never commit real values.

# --- Chrome Web Store (OAuth client + refresh token) ---
# Official docs:
# https://developer.chrome.com/docs/webstore/using_webstore_api
# As of March 10, 2026, the refresh token comes from your Google OAuth setup
# and Chrome Web Store API scope. It does not depend on store approval.
CHROME_EXTENSION_ID=
CHROME_CLIENT_ID=
CHROME_CLIENT_SECRET=
CHROME_REFRESH_TOKEN=
# Local publish script only; CI sets this from the Makefile VERSION:
# CHROME_ZIP_PATH=/absolute/path/to/FontSource/artifacts/fontsource-chrome-1.0.0.zip

# --- Firefox / AMO (JWT credentials; used by mozilla/web-ext as WEB_EXT_*) ---
# From https://addons.mozilla.org/developers/addon/api/key/
# AMO shows "JWT Issuer" + "JWT Secret"; web-ext expects these names:
#   WEB_EXT_API_KEY     = JWT Issuer   (the user id string)
#   WEB_EXT_API_SECRET  = JWT Secret   (the secret string)
# GitHub Actions: add repository secrets with the same variable names.
# Your add-on must declare a stable gecko id for listed updates (see manifest.firefox.json).
WEB_EXT_API_KEY=
WEB_EXT_API_SECRET=

# --- Safari / Mac App Store ---
# Current repo status:
# - GitHub Actions builds and uploads the Safari web-extension source archive only.
# - Automated App Store submission is NOT implemented yet.
# - For shipping to the Mac App Store, Apple expects a signed macOS app archive/upload
#   path using Xcode, altool, or Transporter; API keys alone are not enough.
# Official docs:
# https://developer.apple.com/help/app-store-connect/manage-builds/upload-builds/
# https://developer.apple.com/documentation/appstoreconnectapi/creating-api-keys-for-app-store-connect-api
#
# These are the App Store Connect API key values you would use for future upload automation:
ASC_ISSUER_ID=
ASC_KEY_ID=
# Base64 of the downloaded AuthKey_XXXXXX.p8 private key, or the PEM on one line.
ASC_KEY_P8=
#
# Likely additional Safari CI secrets for a fully automated macOS archive/upload flow:
# APPLE_TEAM_ID=
# APPLE_DEVELOPER_CERT_P12_BASE64=
# APPLE_DEVELOPER_CERT_PASSWORD=
# APPLE_APP_SPECIFIC_PASSWORD=   # if using Transporter/altool with Apple ID auth
# MACOS_PROVISION_PROFILE_BASE64=
